Question
Create a Python function that converts miles to kilometers. You can search the web to find out the conversion formula and then implement it in Python.

Solution: Converter Function

def miles_to_km(m):
     km = m * 1.60934
     return km
 
#Then you can call the function using any value
print(miles_to_km(10))